# AGENTS.md

## 프로젝트 소개

- 이 프로젝트는 2026년 AI 해커톤 제출용 프로젝트다.
- 사용자는 코딩 초보자이므로 작업 내용을 쉬운 한국어로 설명한다.
- 기존 기능을 망가뜨리지 않고 필요한 부분만 수정한다.
- 해양 레저 스포츠를 AI가 비교하고 추천해주는 웹사이트를 제작한다.
- 최종 예약 시 면책 동의서나 환불 규정에 대한 것을 전자서명으로 구현한다.
- 전자 서명 전 AI가 불리한 조항을 요약, 점검 및 주의 해주는 기능을 추가한다.

- [웹 서비스 (유저)]                [AI / Backend]                 [모두싸인 API]
       │                                │                                │
       ├─ (1) 해양 레저 선택 & 예약 ───►│                                │
       │                                ├─ Upstage API로 약관 검토      │
       │◄─ (2) AI 불리한 조항 요약 표시 ┼─ 및 다국어 요약 제공           │
       │                                │                                │
       ├─ (3) 동의 및 서명하기 클릭 ───►│                                │
       │                                ├─ (4) 서명 요청 API 호출 ──────►│ (SECURE_LINK 방식)
       │                                │◄─ (5) 보안 서명 URL 반환 ──────┤
       │◄─ (6) 웹 내 모달로 서명창 띄움 ┤                                │
       │   (유저가 화면에서 바로 서명)  │                                │
       │                                │                                │
       │                                │◄─── (7) 서명 완료 Webhook ─────┤ (상태: COMPLETED)
       │◄─ (8) "예약 완료" 안내 UI ─────┴────────────────────────────────┤

해양 레저를 사용자 취향에 맞게 추천하고 예약하며 안전 사고 및 면책 계약서를 전자서명으로 할 수 있게 하는 웹사이트 제작이 목표

기능 
1. AI가 내 취향과 경제력에 맞는 활동을 추천
2. 해양 레저 선택&예약을 하면 Upstage API로 약관을 검토하고 불리한 조항 요약 표시 및 다국어 요약 제공
3.동의 및 서명하기 클릭 시 서명 요청 API(모두싸인 API) 호출, 보안 서명 URL 반환 후 웹 내 모달로 서명창 띄움(유저가 화면에서 바로 서명)
4. 서명완료 시 예약 완료 안내 UI
5. 이 과정은 판매자와 구매자 각각의 입장의 웹페이지를 제작.
6. 사용가능 API : Upstage API, 모두싸인 API

## 사용 기술

- 새로운 프로그램이나 라이브러리를 추가하기 전에 사용자에게 설명한다.
- 실제로 사용하지 않는 기술을 문서에 적지 않는다.

## 코드 작성 규칙

- 변수와 함수 이름은 역할을 알아보기 쉽게 작성한다.
- 복잡한 코드에는 짧은 설명을 추가한다.
- 같은 기능의 코드를 불필요하게 반복하지 않는다.
- 기존 파일을 함부로 삭제하거나 덮어쓰지 않는다.

## 작업 확인

- 코드를 수정한 뒤 오류가 없는지 확인한다.
- 가능하면 실제 실행 결과를 확인한다.
- 확인하지 못한 부분이 있다면 사용자에게 알려준다.

## Git과 GitHub 규칙

- 사용자의 요청 없이 커밋하거나 GitHub에 올리지 않는다.
- 커밋할 때는 변경 내용을 알 수 있는 이름을 사용한다.
- 관련 없는 파일을 한 커밋에 섞지 않는다.

## 문서 작성

- 실행 방법이 바뀌면 `README.md`도 수정한다.
- AI 사용 방식이 바뀌면 `docs/AI_USAGE.md`에 기록한다.
- 사용한 모델, API 사용 파일, 주요 프롬프트와 설정을 기록한다.

## 보안

- API 키, 비밀번호, 개인정보를 코드나 문서에 작성하지 않는다.
- 비밀값은 `.env` 파일로 관리한다.
- `.env` 파일은 GitHub에 올리지 않는다.

## 작업 완료 보고

작업이 끝나면 다음 내용을 쉬운 한국어로 알려준다.

- 무엇을 만들거나 수정했는지
- 어떤 파일을 수정했는지
- 어떻게 실행하고 확인하는지
- 아직 해결되지 않은 문제가 있는지
